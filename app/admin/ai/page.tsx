import { AiOps } from "@/components/admin/AiOps";

export const metadata = { title: "AI 운영, 백오피스" };

/** 시안 정본 관리자 화면 — 마크업·클래스 그대로 (레이아웃의 .amain 안에 들어간다) */
export default function AiPage() {
  return (
    <>
      <AiOps />
    </>
  );
}
