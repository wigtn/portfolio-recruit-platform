import { CurationBoard } from "@/components/admin/CurationBoard";

export const metadata = { title: "큐레이션 | 백오피스" };

/** 시안 정본 16번(큐레이션) — 배치 변경이 실제로 저장된다 */
export default function CurationPage() {
  return <CurationBoard />;
}
