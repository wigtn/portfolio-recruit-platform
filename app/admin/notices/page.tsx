import { NoticesAdmin } from "@/components/admin/NoticesAdmin";

export const metadata = { title: "공지 · 정책 | 백오피스" };

/** 시안 정본 18번(공지·정책) — 작성·수정·노출 관리가 실제로 동작한다 */
export default function NoticesPage() {
  return <NoticesAdmin />;
}
